export interface Appointment {
  // TODO: Definir campos desde API propia
  // id, propertyId, advisorId, clientName, clientPhone, date, time, status, notes
}

export interface CreateAppointmentPayload {
  // TODO: Payload para crear cita (POST a API + webhook n8n)
  // propertyId, advisorId, clientName, clientPhone, date, time, notes
}
